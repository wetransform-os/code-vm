version: "2"

linters:
  settings:
    errcheck:
      # Terminal output and deferred temp-file cleanup: there is no useful
      # recovery from these failing, and checking them only adds noise.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
        - (*os.File).Close
        - os.Remove
